# Working Preference

- Always execute directly toward a concrete result when feasible, without asking the user to reconfirm steps that are already clear from context.
- Ask only when a missing credential, external authorization, or irreversible decision blocks execution.
